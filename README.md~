# ContactRepo
Add and Search For Contact Names

The project is maven project.To execute follow the command

On console build project 
$ mvn clean install

To run program
$ java -cp target/classes repo.ContactConsoleDashboard
